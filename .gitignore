node_modules/
dist/
*.tsbuildinfo
.DS_Store
.tmp-test/

# packed tarball used by examples/minimal (see `npm run example:install`)
*.tgz
.tmp-bench/
.tmp-simulate/
.tmp-soak/
.tmp-test-one/
.tmp-test-repeat/

# Analysis output written by the reconstructions at game end (see
# examples/rand2011/README.md). Ignored so an interrupted test run cannot leave
# CSVs staged as if they were source.
data/

# simulate output — kept locally, too large and too machine-specific to track
results/

# Per-contributor Claude Code settings. The shared `.claude/` config is tracked;
# this file holds one person's local permissions and paths.
.claude/settings.local.json
